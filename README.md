# Amizone (Amity University Student Portal) CLI App

## Instructions
1. Use `nimble build` to build.
  
2. Set Environment variables `AMIZONE_ID` and `AMIZONE_PASS` with your id and password respectively and then run the built executable file.
